# Log triển khai Service Order issue #16

---

## Thông tin chung

| Trường | Giá trị |
|---|---|
| **Người thực hiện** | Khang |
| **Thời điểm** | 2026-05-21 23:09 |
| **Module** | service-order |
| **Session #** | 001 |
| **Liên quan issue** | [#16](../../issues/16.md) |
| **Liên quan log trước** | Không có |

---

## Code thay đổi từ log trước

Không có.

---

## Những gì đã làm được

- [x] Thêm schema `ServiceOrder` collection `service_orders` với embedded item snapshots.
- [x] Thêm DTO create/add-item/update-item/update-order/query/cancel/response với Swagger decorators.
- [x] Triển khai service logic tạo phiếu, sinh `orderCode`, thêm/sửa/xóa item, tính lại tổng tiền, luồng trạng thái, hủy phiếu và `markAsInvoiced` internal.
- [x] Validate khách hàng active, dịch vụ active và assignment active trước khi snapshot item.
- [x] Triển khai controller 9 endpoint theo issue #16, mutation giới hạn cho role `OPERATOR`/`ADMIN`.
- [x] Wire `ServiceOrderModule` vào `AppModule`, export service + `MongooseModule` cho Invoice #19 inject.
- [x] Thêm error code `SERVICE_ORDER_*`.
- [x] Thêm unit test cho snapshot item, tính lại tổng tiền và status transition chính.
- [x] Smoke test API thật qua JWT OPERATOR trên DB dev: tạo phiếu, thêm/sửa/xóa item, start, complete, cancel, list/detail, invalid transition.
- [x] Cập nhật theo quyết định 2026-05-24: SO start/complete đồng bộ Booking, SO walk-in bỏ qua đồng bộ, thêm audit hủy `cancelledBy`/`cancelReason`.

---

## Thay đổi trong code

### Thêm mới

- `src/modules/service-order/service-order.schema.ts` - schema, enum status, embedded item schema và indexes.
- `src/modules/service-order/service-order.service.ts` - business logic Phiếu dịch vụ.
- `src/modules/service-order/service-order.controller.ts` - REST endpoints.
- `src/modules/service-order/service-order.module.ts` - module wiring/dependencies.
- `src/modules/service-order/dto/*.ts` - DTO request/response/query.
- `src/modules/service-order/service-order.service.spec.ts` - unit test cho logic chính.
- `ai-docs/modules/service-order/README.md` - tài liệu hiện trạng module.

### Sửa đổi

- `src/app.module.ts` - import `ServiceOrderModule`.
- `src/shared/constants/error-codes.ts` - thêm error code Service Order.
- `ai-docs/STATUS.md` - set active row issue #16.
- `ai-docs/TIMELINE.md` - thêm row log issue #16.
- `ai-docs/issues/16.md` - link log liên quan.
- `src/modules/service-order/service-order.module.ts` - inject thêm `Booking` model để đồng bộ trạng thái theo phương án C2.
- `src/modules/service-order/service-order.service.ts` - chạy transaction khi SO chuyển `IN_PROGRESS`/`COMPLETED` và đồng bộ Booking trong cùng transaction.

### Xóa

Không có.

---

## Lý do thay đổi

- Issue #16 là dependency trực tiếp để Invoice #19 có dữ liệu Phiếu dịch vụ hoàn chỉnh: snapshot dịch vụ, snapshot chuyên viên, commission rate và total amount.
- Tách tạo phiếu rỗng và thêm item giúp Booking #17 có thể check-in tạo Phiếu dịch vụ trước, sau đó nhân viên vận hành thêm dịch vụ.
- Sau quyết định 2026-05-24, Phiếu dịch vụ là chủ vòng đời phục vụ sau check-in; Booking tự đồng bộ theo SO để dữ liệu demo không bị lệch.

---

## Vướng mắc / Quyết định kỹ thuật

- Smoke test DB dev phát hiện seed customer cũ thiếu `fullName`; smoke test dùng customer mới tạo qua API để verify populate `customer.fullName`.
- `seed:assignments` đang dừng ở service `NECK_SHOULDER_30` do dữ liệu service hiện có trong DB dev lệch/inactive; smoke test dùng `SWEDISH_60` đã có assignment active.
- Dùng camelCase cho field thường theo hiện trạng codebase sau log service `2026-05-13_002_Khanh`; không dùng `@Prop({ name })`.
- `UpdateItemDto` nhận `serviceId` optional rồi service ignore để request cũ không fail do global `forbidNonWhitelisted=true`.
- Đồng bộ SO -> Booking dùng `BookingModel` trực tiếp trong `ServiceOrderModule`, không inject ngược `BookingService`, để tránh circular dependency.

---

## Bước tiếp theo

- [x] Booking #17 triển khai check-in, gọi `ServiceOrderService.create` với `bookingId`.
- [ ] Khi Invoice #19 triển khai, gọi `markAsInvoiced(orderId, invoiceId, session)` trong transaction.

---

## Trạng thái code changes

- [ ] Không có code changes - log tiếp theo không cần ghi lại
- [x] Có code changes - log tiếp theo cần điền section "Code thay đổi từ log trước" nếu khác dev
