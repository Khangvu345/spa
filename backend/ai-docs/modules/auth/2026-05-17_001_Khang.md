# Log 2026-05-17_001_Khang - Refactor role vận hành về OPERATOR

## Issue

- [#08](../../issues/08.md) - Refactor role vận hành về 3 roles + seed staff demo

## Phạm vi

- Module chính: `auth`, `employee/employee`
- Module bị ảnh hưởng bởi quyền "mọi role JWT": `supplier`, `material`
- Không sửa nội dung snapshot cũ trong `ai-docs/issues/*.md`; issue #08 là nơi ghi quyết định supersede phần role enum cũ.

## Nội dung đã làm

- Chốt role hệ thống còn 3 giá trị: `ADMIN`, `OPERATOR`, `STAFF`.
- `OPERATOR` thay thế toàn bộ nghiệp vụ lễ tân và thu ngân.
- Giữ script migration `migrate:staff-role-operator` để chuyển dữ liệu cũ `RECEPTIONIST`/`CASHIER` sang `OPERATOR`.
- Thêm script seed staff demo theo 3 role mới: `npm run seed:staff`.
- Cập nhật README các module bị ảnh hưởng để link về log refactor này.
- Ghi timeline cho issue #08.

## Issue bị ảnh hưởng

| Issue | Ảnh hưởng | Ghi chú xử lý |
|---|---|---|
| #02 | Staff schema/RBAC cũ ghi 4 role | Code và context mới supersede bằng `ADMIN | OPERATOR | STAFF` |
| #03 | Employee CRUD tạo/sửa/lọc theo role | DTO và Swagger example dùng `OPERATOR` |
| #04 | Account management dùng JWT/RBAC | Không đổi endpoint; token sau login dùng role mới |
| #06 | Supplier GET mọi role JWT | README module ghi operator/staff thay cho receptionist/staff |
| #07 | Material GET mọi role JWT | README module ghi operator/staff thay cho receptionist/staff |

## Ghi chú vận hành

- DB dev cũ cần chạy `npm run migrate:staff-role-operator` trước khi test quyền.
- Seed demo mới chạy bằng `npm run seed:staff`; password mặc định lấy từ `SEED_STAFF_PASSWORD`.
- Không dùng `RECEPTIONIST`/`CASHIER` trong logic mới, trừ script migration và ghi chú lịch sử.

## Kiểm tra

- `nest build` pass.
- `jest --runInBand` pass: 5 suites, 36 tests.
