# Log Entry — Implement Customer CRUD

---

## Thông tin chung

| Trường | Giá trị |
|---|---|
| **Người thực hiện** | Khang |
| **Thời điểm** | 2026-05-21 08:08 |
| **Module** | customer |
| **Session #** | 001 |
| **Liên quan Issue** | [#14](../../issues/14.md) |
| **Liên quan log trước** | Không có |

---

## Code changes từ log trước

Không có.

---

## Những gì đã làm được

- [x] Tạo schema `Customer` với unique phone, OTP placeholder fields, soft delete flag và indexes.
- [x] Tạo DTO create, find-or-create, update, query, response với Swagger decorators.
- [x] Implement service CRUD, public find-or-create by phone, search/filter/sort/pagination, duplicate/inactive/not-found error codes.
- [x] Implement controller endpoints và role/public decorators theo issue #14.
- [x] Thêm seed script idempotent `npm run seed:customers`.
- [x] Wire `CustomerModule` vào `AppModule`.
- [x] Thêm unit test cho `CustomerService` covering CRUD rules chính.

---

## Thay đổi / Xóa trong code

### Thêm mới
- `src/modules/customer/customer.schema.ts` — schema và indexes collection `customers`.
- `src/modules/customer/customer.module.ts` — khai báo module, export service + MongooseModule.
- `src/modules/customer/customer.controller.ts` — endpoints CRUD/find-or-create/toggle-active.
- `src/modules/customer/customer.service.ts` — business logic customer.
- `src/modules/customer/parse-customer-phone.pipe.ts` — validate param phone cho lookup nhanh.
- `src/modules/customer/dto/*.ts` — DTOs cho request/response/query.
- `src/modules/customer/customer.service.spec.ts` — unit test cho duplicate phone, find-or-create, search/filter/sort, update ignore phone, toggle active.
- `src/scripts/seed-customers.ts` — seed 8 khách hàng mẫu bằng upsert theo phone.

### Sửa đổi
- `src/app.module.ts` — import `CustomerModule`.
- `src/shared/constants/error-codes.ts` — thêm `CUSTOMER_PHONE_EXISTS`, `CUSTOMER_INACTIVE`.
- `package.json` — thêm script `seed:customers`.

### Xóa
- Không có.

---

## Lý do thay đổi

- Issue #14 yêu cầu Customer CRUD đầy đủ để unblock các module Booking, Service Order và Invoice dùng `customerId` ref.

---

## Vướng mắc / Quyết định kỹ thuật

- Issue có một điểm mâu thuẫn nội bộ: acceptance chính yêu cầu OTP fields default `false/null`, trong context bổ sung có câu OPERATOR tạo thì `phoneVerified=true`. Session này bám acceptance chính để giữ schema/service default `false/null`.
- DTO update accept `phone` optional rồi service ignore để đáp ứng acceptance "PATCH gửi field phone → bị ignore" trong khi global ValidationPipe đang bật `forbidNonWhitelisted`.
- Verification dùng `fnm exec --using 24.14.0 npm.cmd ...`: build pass, test pass 1 suite/10 tests, seed customer idempotent pass.

---

## Bước tiếp theo

- [ ] Sau khi PR/merge xong, clear row trong `STATUS.md` và đổi issue status nếu cần.

---

## Trạng thái code changes

- [ ] ✅ Không có code changes — log tiếp theo không cần ghi lại
- [x] ⚠️ Có code changes — log tiếp theo cần điền section "Code changes từ log trước" nếu khác dev
