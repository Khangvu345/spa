# Log Entry - Implement Employee CRUD trên Staff schema

---

## Thông tin chung

| Trường | Giá trị |
|---|---|
| **Người thực hiện** | Khang |
| **Thời điểm** | 2026-05-13 11:35 |
| **Module** | employee/employee |
| **Session #** | 001 |
| **Liên quan Issue** | [#03](../../../issues/03.md) |
| **Liên quan log trước** | Không có |

---

## Code changes từ log trước

Không có.

---

## Những gì đã làm được

- [x] Thêm DTO create/update/query/response cho Employee CRUD.
- [x] Thêm `EmployeeController` với POST/GET list/GET detail/PATCH dùng route `/employees`.
- [x] Mở rộng `EmployeeService` với create/list/detail/update trên `Staff` model.
- [x] Hash password khi tạo nhân viên và check duplicate email trả `STAFF_EMAIL_EXISTS`.
- [x] List hỗ trợ pagination default limit 10, search fullName/email, filter role/workStatus, sort fullName/startedAt asc/desc.
- [x] PATCH chỉ apply field được phép, không apply email/password/accountStatus.
- [x] Thêm/mở rộng unit test service cho create duplicate/list/update/password support.
- [x] Chạy verification bằng Node/NPM do `fnm` kích hoạt.
- [x] `npm run build` pass.
- [x] `npm test -- employee.service.spec.ts` pass.
- [x] `npm test` pass: 4 suites, 20 tests.

---

## Thay đổi / Xóa trong code

### Thêm mới
- `src/modules/employee/employee/employee.controller.ts` - controller Employee CRUD.
- `src/modules/employee/employee/strip-employee-credentials.interceptor.ts` - strip email/password trên PATCH employee trước validation.
- `src/modules/employee/employee/dto/create-employee.dto.ts` - DTO tạo nhân viên.
- `src/modules/employee/employee/dto/update-employee.dto.ts` - DTO cập nhật nhân viên, omit email/password.
- `src/modules/employee/employee/dto/query-employee.dto.ts` - DTO query list/search/filter/sort.
- `src/modules/employee/employee/dto/employee-response.dto.ts` - response DTO cho Employee CRUD có `lockedAt`.

### Sửa đổi
- `src/modules/employee/employee/employee.module.ts` - đăng ký `EmployeeController`.
- `src/modules/employee/employee/employee.service.ts` - thêm CRUD Employee trên Staff model.
- `src/modules/employee/employee/employee.service.spec.ts` - thêm test cho Employee CRUD service.

### Xóa
- Không có.

---

## Lý do thay đổi

- Issue #03 yêu cầu CRUD Employee đầy đủ trên Staff schema đã có từ #02, không tạo endpoint delete và không sửa Staff schema.
- Email/password không được sửa qua PATCH employee vì đổi email out of scope, đổi password nằm ở auth self-change hoặc issue #04 admin reset.

---

## Vướng mắc / Quyết định kỹ thuật

- Theo issue, `GET /employees` và `GET /employees/:id` chỉ cần JWT, không gắn `@Roles`.
- RỦI RO cần chốt với Vu/Khang: STAFF xem record của người khác có thể là horizontal privilege escalation. Session này giữ đúng acceptance criteria, chưa restrict detail endpoint.
- Verification phải init `fnm` thủ công trong shell sandbox vì PowerShell profile không load được.
- Repo convention: tiếng Việt trong code/docs phải viết có dấu.

---

## Bước tiếp theo

- [ ] Nếu Vu muốn restrict detail, thêm rule STAFF chỉ xem chính mình và test 403/404 tương ứng.
- [ ] Làm issue #04 cho reset password, lock/unlock, delete account.

---

## Trạng thái code changes

- [ ] Không có code changes - log tiếp theo không cần ghi lại
- [x] Có code changes - log tiếp theo cần điền section "Code changes từ log trước" nếu khác dev
