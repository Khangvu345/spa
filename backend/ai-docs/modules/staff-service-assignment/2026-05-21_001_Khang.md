# Log Entry - Triển khai staff_service_assignments issue #15

---

## Thông tin chung

| Trường | Giá trị |
|---|---|
| **Người thực hiện** | Khang |
| **Thời điểm** | 2026-05-21 21:49 |
| **Module** | staff-service-assignment |
| **Session #** | 001 |
| **Liên quan Issue** | [#15](../../issues/15.md) |
| **Liên quan log trước** | Không có |

---

## Code changes từ log trước

Không có.

---

## Những gì đã làm được

- [x] Thêm schema `StaffServiceAssignment` cho collection `staff_service_assignments`.
- [x] Thêm DTO create/query/update/response với Swagger decorators.
- [x] Thêm service logic: create, findAll, findByService, findByStaff, findOne, update.
- [x] Validate staff phải là `STAFF` active và service phải active khi tạo mapping.
- [x] Enforce mỗi service chỉ có 1 assignment active bằng unique partial index.
- [x] Thêm controller 5 endpoint theo issue #15, POST/PATCH chỉ ADMIN.
- [x] Thêm script `seed-staff-service-assignments.ts` và command `npm run seed:assignments`.
- [x] Cập nhật `seed-staff.ts` để có đủ 6 STAFF theo issue #15.
- [x] Thêm unit test cho các rule chính của `StaffServiceAssignmentService`.

---

## Thay đổi / Xóa trong code

### Thêm mới
- `src/modules/staff-service-assignment/staff-service-assignment.schema.ts` - schema + indexes.
- `src/modules/staff-service-assignment/staff-service-assignment.service.ts` - business logic assignment.
- `src/modules/staff-service-assignment/staff-service-assignment.controller.ts` - REST endpoints.
- `src/modules/staff-service-assignment/staff-service-assignment.module.ts` - module wiring.
- `src/modules/staff-service-assignment/dto/*.ts` - DTO request/response.
- `src/modules/staff-service-assignment/staff-service-assignment.service.spec.ts` - unit tests.
- `src/scripts/seed-staff-service-assignments.ts` - seed 6 mapping cố định.
- `ai-docs/modules/staff-service-assignment/README.md` - current snapshot module.

### Sửa đổi
- `src/app.module.ts` - import `StaffServiceAssignmentModule`.
- `src/shared/constants/error-codes.ts` - thêm error codes assignment.
- `src/scripts/seed-staff.ts` - cập nhật seed staff demo thành 1 admin, 1 operator, 6 staff theo issue #15.
- `package.json` - thêm script `seed:assignments`.
- `ai-docs/STATUS.md` - set row Khang đang code issue #15.
- `ai-docs/TIMELINE.md` - thêm row log issue #15.
- `ai-docs/issues/15.md` - link log liên quan.

### Xóa
- Không có.

---

## Lý do thay đổi

- Issue #15 cần mapping chuyên viên - dịch vụ - hoa hồng làm dependency cho Booking/Service Order/Invoice.
- Context bổ sung của issue chốt reassign bằng deactivate assignment cũ rồi tạo assignment mới để không làm sai dữ liệu lịch sử.
- Seed assignments phải lookup `staffId`/`serviceId` runtime, không hardcode ObjectId.

---

## Vướng mắc / Quyết định kỹ thuật

- Module dùng field thường camelCase theo hiện trạng codebase sau log service `2026-05-13_002_Khanh`; collection vẫn là `staff_service_assignments`, timestamps vẫn `created_at`/`updated_at`.
- `UpdateAssignmentDto` không nhận `staffId`/`serviceId` dù phần checklist ban đầu có nhắc, vì context bổ sung issue #15 chốt không sửa staffId khi reassign.
- Chưa chạy smoke test với DB dev/seed thật trong phiên này.

---

## Bước tiếp theo

- [ ] Smoke test Postman các endpoint issue #15 với JWT ADMIN/non-ADMIN.
- [ ] Chạy `npm run seed:services`, `npm run seed:staff`, `npm run seed:assignments` trên DB dev.
- [ ] Khi làm Invoice #19, snapshot commission rate tại thời điểm tạo dữ liệu tài chính.

---

## Trạng thái code changes

- [ ] Không có code changes - log tiếp theo không cần ghi lại
- [x] Có code changes - log tiếp theo cần điền section "Code changes từ log trước" nếu khác dev
