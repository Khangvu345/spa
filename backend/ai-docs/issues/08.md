---
id: 08
title: Refactor role vận hành về 3 roles + seed staff demo
module: auth, employee/employee
paste_date: 2026-05-17
pasted_by: Khang
status: done
---

# Issue #08: Refactor role vận hành về 3 roles + seed staff demo

> Snapshot của issue tại thời điểm dev paste vào chat AI.

---

## Nội dung Issue (paste nguyên văn)

kiểm tra lại lần cuối về việc refactor về 3 roles đang ảnh hưởng tới các issue nào

tổng hợp logs về vấn đề này vào từng readme của các module bị ảnh hưởng cũng như ghi log issue vấn đề refactor này vào timeline

Sau đó thực hiện tạo script seed staff 

---

## Context bổ sung từ dev

- Scope chốt chỉ còn 3 role hệ thống: `ADMIN`, `OPERATOR`, `STAFF`.
- `OPERATOR` là role vận hành gộp lễ tân và thu ngân.
- Không dùng lại `RECEPTIONIST` hoặc `CASHIER` trong logic mới.
- Dữ liệu cũ trong DB nếu còn `RECEPTIONIST`/`CASHIER` phải chạy migration để chuyển về `OPERATOR`.
- Cần gom dấu vết refactor vào README của các module bị ảnh hưởng và Timeline để AI/dev sau không đọc nhầm issue snapshot cũ.

---

## Phạm vi ảnh hưởng

| Issue | Mức ảnh hưởng | Hướng xử lý |
|---|---|---|
| #02 Auth core + Staff schema | `StaffRole` trong snapshot cũ có `RECEPTIONIST`/`CASHIER` | Issue #08 supersede phần role enum; code hiện dùng `ADMIN | OPERATOR | STAFF` |
| #03 Employee CRUD | DTO create/update/query/filter role phụ thuộc `StaffRole` | Swagger example và enum runtime dùng `OPERATOR`; filter role chỉ nhận 3 role mới |
| #04 Account management | JWT/RBAC và `mustChangePassword` phụ thuộc role trong token | Không đổi flow; chỉ đảm bảo token dùng role mới sau login |
| #06 Supplier | GET list/detail ghi "mọi role JWT", note cũ nhắc receptionist/staff | README module dùng operator/staff; issue snapshot giữ nguyên |
| #07 Material | GET list/detail ghi "mọi role JWT", logic nội bộ cần operator/staff tra cứu vật liệu | README module dùng operator/staff; issue snapshot giữ nguyên |

---

## Checklist hoàn thành

- [x] `StaffRole` chỉ còn `ADMIN`, `OPERATOR`, `STAFF`.
- [x] DTO employee dùng `StaffRole.OPERATOR` làm example cho role vận hành.
- [x] Thêm script migration `migrate:staff-role-operator`.
- [x] Thêm script seed staff demo theo 3 role mới.
- [x] Cập nhật README module bị ảnh hưởng.
- [x] Ghi log refactor vào Timeline.

---

## Logs liên quan

- [2026-05-17_001_Khang](../modules/auth/2026-05-17_001_Khang.md) — Refactor role vận hành về OPERATOR + seed staff demo

---

## Trạng thái

- `done` — đã chuẩn hóa tài liệu và script seed staff cho 3 role.
